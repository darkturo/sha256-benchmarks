NAME=rust-sha2-$(grep sha2 Cargo.toml | cut -d" " -f3 | sed 's/"//g')
TYPE=bin
INPUT=abc
COMMAND=rust-sha2-bench
SKIP=$(command -v rustc >/dev/null 2>&1 && command -v cargo >/dev/null 2>&1 && echo NO || echo YES)
